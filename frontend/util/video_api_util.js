export const fetchFilteredVideos = (videos, filter) => ({

})

export const fetchVideo = video => ({

})

export const postVideo = video => ({

})

export const patchVideo = video => ({

})

export const deleteVideo = () => ({

})
